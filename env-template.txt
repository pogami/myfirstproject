# Copy this to .env.local for local development
# Copy this to Vercel environment variables for production

# Firebase Client Configuration (add your actual keys from Firebase Console)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket_here
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id_here

# Firebase Admin SDK (Server-side only - KEEP PRIVATE!)
# Get these from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your_firebase_project_id_here
FIREBASE_CLIENT_EMAIL=your_firebase_client_email_here
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----"

# AI API Keys (Server-side only - KEEP PRIVATE!)
GOOGLE_AI_API_KEY=your_google_ai_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
GEMINI_MODEL=gemini-1.5-flash  # Options: gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash-exp, etc.

# Real-Time Web Search (Server-side only - KEEP PRIVATE!)
# Get these from: https://console.cloud.google.com/ (API Key) and https://cse.google.com/ (Search Engine ID)
GOOGLE_SEARCH_API_KEY=your_google_search_api_key_here
GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id_here
SEARCH_RESULTS_LIMIT=12  # Optional: Number of results (1-20, default: 12)

# Admin Password (for admin and feedback pages)
ADMIN_PASSWORD=your_admin_password_here

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Add these variables to Vercel for production
# 3. Never commit .env.local to GitHub
# 4. The NEXT_PUBLIC_ variables are safe to expose (client-side)
# 5. The non-NEXT_PUBLIC_ variables are private (server-side only)
# 6. Get Google AI API key from: https://aistudio.google.com/app/apikey
# 7. Get OpenAI API key from: https://platform.openai.com/api-keys
# 8. For Firebase Admin SDK, go to Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# 9. Get Google Search API key from: https://console.cloud.google.com/ → Enable "Custom Search API" → Create API Key
# 10. Get Search Engine ID from: https://cse.google.com/ → Create search engine → Copy "Search engine ID"
